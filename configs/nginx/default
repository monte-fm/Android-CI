server {
        server_name localhost;
        root /var/www;
	
          client_max_body_size 40m;

          location / {
                autoindex on;
                index index111.html index111.htm;
          }

          error_log /var/log/nginx/.error.log;
          access_log /var/log/nginx/.access.log;

	                 
}
